# Filesystem inventory

Conclusion: [CONFIRMED] The extracted root contains 4,360 inodes: 3,711 files, 235 directories, 648 symbolic links, and one device entry in the original SquashFS.

Evidence: sorted manifests under `analysis/manifests/`, generated by `scripts/inventory-filesystem.sh` and `scripts/find-modem-components.sh`.

Confidence: confirmed.

Alternative explanations: owners are those represented by SquashFS metadata inside the Linux extraction container. The macOS inspection copy cannot represent the device node.

How to verify dynamically: `make inventory`; compare `analysis/manifests/filesystem.tsv`, `file-types.txt`, `elf-paths.txt`, and `service-and-interface-paths.txt`.

High-value modem components include:

| Layer | Paths |
|---|---|
| Resident manager | `/usr/bin/cellular_manager`, `/etc/init.d/gl_cellular_manager` |
| CLI and helpers | `/usr/bin/gl_modem`, `/usr/bin/modem_AT`, `/usr/bin/qcm`, `/usr/bin/process_modem_network`, `/usr/bin/modem_signal` |
| Shared backend | `/usr/lib/libcm*.so` |
| HTTP/RPC | `/usr/lib/oui-httpd/rpc/modem.so`, `/usr/share/gl-ngx/oui-rpc.lua` |
| Websocket | `/usr/share/gl-ngx/websocket/cellular.lua` |
| Detection | `/etc/hotplug.d/usb/30_modem`, `/lib/modem_data/modem_list.json` |
| Network | `/lib/netifd/proto/{qcm,qmi,ncm,3g,wwan}.sh` |

The extracted root and proprietary binaries are excluded from Git. Only hashes, metadata, string catalogs, and reports are tracked.
